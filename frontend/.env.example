# API Base URL
# For local development on the same machine, use: http://localhost:3000/api
# For network access (e.g., phone accessing from same WiFi), use your computer's IP address
# Example: http://192.168.1.100:3000/api (replace with your actual IP)
VITE_API_BASE_URL=http://localhost:3000/api
